# Copy this file to `.private-sync.env` and keep the copy untracked.
PRIVATE_SYNC_REMOTE_URI=gs://example-private-bucket/private/
PRIVATE_SYNC_LOCAL_DIR=.private

# Set to 1 only when the remote should delete files absent locally.
PRIVATE_SYNC_DELETE=0
