// Copy this to frontend/config.js to configure local development
// If left empty or not present, the frontend will fall back to the public proxy
window.POW_CONFIG = {
  // Example: "http://localhost:8000"
  apiBase: "",
  // Optional: Google Maps Static Street View API key for popup thumbnails
  // Leave empty to disable thumbnails (links will still be shown)
  googleMapsApiKey: ""
};

